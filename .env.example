# -------------------------------------------------------------------
# Kimi — Environment Variables
# Copy this file to .env and fill in real values. NEVER commit .env.
# -------------------------------------------------------------------

# ===== SUPABASE (used by frontend + backend) =====
# Project URL from Supabase Dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
# Anon (public) key — safe to expose to browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key...

# Service-role key — SERVER ONLY, never expose to browser
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key...

# Direct DB connection string (for Prisma / migrations)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_ID.supabase.co:5432/postgres"

# ===== FRONTEND (packages/web) =====
# Public URL of your deployment
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Public URL of the API (if using packages/api separately)
NEXT_PUBLIC_API_URL=http://localhost:4000

# ===== BACKEND API (packages/api) =====
# Clerk keys (LEGACY — being replaced by Supabase Auth)
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=
# CLERK_WEBHOOK_SECRET=

# ===== ESPORTS DATA =====
# API key for PandaScore / Abios / other data provider
ESPORTS_API_KEY=

# ===== ML MODEL =====
MODEL_DIR=ml/models
PROJECTION_STAT_TYPE=Kills
STATS_CSV_PATH=ml/data/match_stats.csv
SCHEDULE_CSV_PATH=ml/data/schedule.csv
SCHEDULE_HORIZON_HOURS=0
MATCH_LIVE_LEAD_MINUTES=0
MATCH_AUTOCOMPLETE_MINUTES=90
MATCH_ALLOW_ROLLBACK=0
